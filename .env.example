# Copia este fichero a .env y rellena los valores antes de levantar el stack.

# PostgreSQL
DB_PASS=cambia_esto
AUTH_PASS=cambia_esto_para_postgrest

# Secreto HMAC para los JWT (mismo en Postgres y PostgREST). Mínimo 32 chars.
JWT_SECRET=cambia_esto_minimo_32_caracteres_aleatorios

# Contraseña inicial del usuario admin de la app (se aplica solo en el primer init).
ADMIN_PASS=cambia_esto_minimo_8

# pgAdmin (interfaz web detrás de Traefik)
PGADMIN_EMAIL=tu@correo.es
PGADMIN_PASS=cambia_esto
DOMINIO_PGADMIN=pgadmin.aprentix.es
DOMINIO_API=api.aprentix.es
