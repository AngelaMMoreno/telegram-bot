# Stack WEB — SPA de tests en test.aprentix.es / www.test.aprentix.es.
# Redespliega cuando toques web/site/* o su Caddyfile.
# Habla con PostgREST (stack core) por http://postgrest:3000.

services:

  web:
    build: ../../web
    restart: unless-stopped
    networks: [dokploy-network]
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.web.rule=Host(`${DOMINIO_WEB:-test.aprentix.es}`) || Host(`${DOMINIO_WEB_ALT:-www.test.aprentix.es}`)"
      - "traefik.http.routers.web.entrypoints=websecure"
      - "traefik.http.routers.web.tls=true"
      - "traefik.http.routers.web.tls.certresolver=letsencrypt"
      - "traefik.http.services.web.loadbalancer.server.port=80"

networks:
  dokploy-network:
    external: true
